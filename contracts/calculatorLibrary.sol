








//     // SPDX-License-Identifier: Sharia
// pragma solidity ^0.8.24;


//  abstract contract  LibRegistry {



// int sumBasis =0;
//     for(uint j; j < length;){
//         for(uint i; i < 4;){
//             uint midHash =  MainRegistry[j].GetHashByIndex(i);
//             address class =  MasterMap[j][midHash]; 
//             uint uHash = class.hashWith(user[i]);
//                 UserMainMap[j][midHash][uHash];

//                 MainRegistry[j][midHash].Rigister(uHash);
//                 UserMainMap[j][midHash][uHash]   =  UserMainMap[j][midHash].calculateTotalBasisFee(user, amount);
//                    sumBasis +=   UserMainMap[j][midHash][uHash];
//                  MainRegistry[stuff][class.hashWith(user)];   
//         }


// }

// |}